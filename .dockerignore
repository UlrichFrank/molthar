node_modules
*/node_modules
**/node_modules

# Build artifacts (rebuilt inside Docker)
*/dist
shared/dist
backend/dist
game-web/dist

# Dev/local files not needed in image
.env
.env.local
*.log
.DS_Store
.git
openspec

# Source-only / unused heavy assets (not referenced by the app)
assets/*.psd
assets/*.af
assets/Anleitung*

# Tests never run inside the image
**/*.test.ts
